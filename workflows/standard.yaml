name: standard
description: Standard workflow with all core phases - recommended for most stories

phases:
  - planner
  - coder
  - reviewer
  - tester

retry_policy:
  planner:
    max_attempts: 3
  coder:
    max_attempts: 3
  reviewer:
    max_attempts: 3
  tester:
    max_attempts: 3
